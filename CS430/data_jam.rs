#[derive(Debug)]
Struct Country {
    country: String,
    capital: String,
    latitude: f32,
    longitude: f32,
    capital population: u32,
    area: f32,
    pop_1960: u32,
    pop_2021: u32,
}

fn main() {
    let std::fs::read_to_string
    let mut countries: Vec<Country> = 
}